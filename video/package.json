{
  "name": "mhizha-video",
  "version": "0.0.0",
  "private": true,
  "description": "Remotion rendering of the Mhizha submission video. This is the SUBMITTED build; scripts/video_render.py renders the alternate.",
  "scripts": {
    "material": "node scripts/material.mjs",
    "studio": "remotion studio src/index.ts",
    "render": "remotion render src/index.ts Mhizha out/remotion.mp4",
    "build": "npm run material && npm run render && npm run mux",
    "mux": "../.venv/bin/python ../scripts/video_render.py --mux out/remotion.mp4"
  },
  "dependencies": {
    "@remotion/cli": "4.0.290",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "remotion": "4.0.290"
  }
}
